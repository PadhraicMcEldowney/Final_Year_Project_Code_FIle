#import random
#import matplotlib.pyplot as plt

#def generate_humidity_graph():
    # Generate some random temperature data for the last 24 hours
    #humid_data = [random.randint(35, 60) for _ in range(24)]

    # Create the graph
    #fig, ax = plt.subplots()
    #ax.plot(humid_data)
    #ax.set_xlabel('Hour of the day')
    #ax.set_ylabel('Humidity %')
    #ax.set_title('Humidity for the last 24 hours')

    # Save the graph as a PNG image
    #fig.savefig('static/humidity_graph.png')

    # Return the path to the graph image
    #return 'static/humidity_graph.png'