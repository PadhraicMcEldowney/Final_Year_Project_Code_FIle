#import random
#import matplotlib.pyplot as plt

#def generate_voc_graph():
    # Generate some random temperature data for the last 24 hours
    #voc_data = [random.uniform(0.1, 0.6) for _ in range(24)]

    # Create the graph
    #fig, ax = plt.subplots()
    #ax.plot(voc_data)
    #ax.set_xlabel('Hour of the day')
    #ax.set_ylabel('VOCs (ppm)')
    #ax.set_title('VOCs for the last 24 hours')

    # Save the graph as a PNG image
    #fig.savefig('static/voc_graph.png')

    # Return the path to the graph image
    #return 'static/voc_graph.png'